# /addons/whatsapp_client/config.yaml
name: "WhatsApp Client"
version: "2.0.4"
slug: "whatsapp_client"
description: "WhatsApp client for HA — Baileys engine (no Chromium, lightweight)"
arch:
  - armv7
  - aarch64
  - amd64
init: false
homeassistant_api: true
ingress: true
ingress_port: 3001
panel_icon: "mdi:whatsapp"
panel_title: "WhatsApp"
host_network: true
options:
  TEST_MESSAGE: true
  RESTART_HOURS: 0
  SAFE_MODE: false
  MIGRATION_FLAG: ".migrated_v200_baileys"
schema:
  TEST_MESSAGE: bool
  RESTART_HOURS: int
  SAFE_MODE: bool
  MIGRATION_FLAG: str
